doctype html
html
  head
    title CoastCast
    link(rel='stylesheet', href='/css/app.css')
    link(rel='shortcut icon', href='/favicon.ico')
    
  body
    .header
      span.nav-title
        h1 CoastCast
      span.nav-title
        h3 #{reportType}: #{reportCount} Total
    .content
      != message
      block content
      if (userCount === 1)
        p #{userCount} user connected
      else
        p #{userCount} Users connected
      
    .footer
      include ../includes/submit

  script(src="https://cdn.socket.io/socket.io-1.3.5.js")
  script.
    var socket = io();
